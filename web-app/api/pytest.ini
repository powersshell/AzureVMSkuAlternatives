[pytest]
testpaths = tests
markers =
    unit: Unit tests (no network required)
    cache: Cache validation tests (requires Azure Table Storage credentials)
    api: API contract tests (requires live API endpoint)
timeout = 120
